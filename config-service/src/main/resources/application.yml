spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///E:/config/licensingservice
        encrypt:
          enabled: false
server:
  port: 8888

